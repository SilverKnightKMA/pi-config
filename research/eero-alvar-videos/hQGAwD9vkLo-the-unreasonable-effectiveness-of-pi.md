# The Unreasonable Effectiveness of Pi

Video: https://www.youtube.com/watch?v=hQGAwD9vkLo

## Transcript

Coding agents are not just for code. They're the primary medium through which you interact with and harness the power of LLMs. Coding agents are for leverage. They will be the tools for doing stuff at scale. I've used most of the major coding agents out there and to me at least the choice is obvious. The open source, fully customizable, and infinitely extensible minimal agent harness. The neovim of coding agents. Of course, I'm talking about pup. >> [music] >> Your tools should not be the bottleneck. Cloud code, cursor, open code, any major coding assistant today, they're really great. They're amazing out of the box, but they were not made for you. They were made for the average coder, the average user, and for that reason they drive like automatics with software separating you from the road. This leads to dark matter. All kinds of invisible behaviors, hidden context injections, and other undocumented features that the coding agents run behind your back. You can't see it, you can't control it, but it shapes every response your agent gives you. Let's say you build a workflow that works. Then the next update changes some hidden behavior, some context management, or compaction stuff, and as a result your workflow breaks or the behavior changes meaningfully, and you didn't change anything. They did. You're sort of building on someone else's foundation and they keep changing stuff without telling you. Now, what benchmarks like the terminal bench have showed us is that the features are the drivers of performance. The underlying model, the LLM, does the heavy lifting and the agency harness just kind of needs to stay out of the way. It needs to be a minimal. We need manual transmission with no power steering, total control. All right, so here's the framework for what the ideal coding agent should look like. First, minimal core. The agent needs almost nothing. Basically just ways to read files, write files, and run commands. The frontier models are already trained with reinforcement learning to be coding agents, pretty much. They don't need another 2,000 line system prompt telling them how to behave. A short, clean prompt is sufficient. Plus, it keeps the agent general and customizable without adding unnecessary bloat. Everything beyond that is overhead at best and interference at worst. Next, total transparency. No secretly injected context, no undocumented behaviors. You should have full knowledge of everything going on at all times. You need to be in the driver's seat. Thirdly, extensibility as architecture, not some settings you can toggle. Real extensibility. You should be able to build on top of the agent harness. You should be able to create custom agents optimized for anything. Custom tools, custom UI, custom behaviors, custom everything. The agent should be a blank slate. We maximize the outputs and the results by optimizing the agents for our task and our task only, and it requires full customizability. So, in fact, what we need is not an agent, it's just the harness, a pure harness. It doesn't have opinions, it doesn't come preloaded with behaviors. We need just the minimal infrastructure for an LLM to act in the world and everything beyond that is for you to define. This agent exists. It is Pi. There are many coding agents, but this one is mine. The perfect catchphrase. It's a terminal-based pure agent harness. It has only four built-in tools: read, write, edit, and bash. It's got a system prompt that's under 200 tokens long. This is the system prompt. That's it. Compare this to Claude code's around 10,000 token system prompt. These long system prompts used to be more relevant when the LLMs weren't as smart, but nowadays the LLMs already know what a coding agents is pretty much from RL training. So, this under 200 token system prompt is all that's required. This keeps Pi lightweight and very generalizable. You shouldn't be forced to load your context window with 10,000 tokens of coding best practices when you just want to do some Obsidian vault analysis, for example. Next, Pi has no safety mode. It's YOLO mode by default and that's a good thing. Safety features tend to only limit the agent's true potential. So, Pi sees everything on your computer. It has access to everything. It can do whatever it wants, meaning it can delete everything if it wants to, but Pi assumes that you know what you're doing. And of course, you can and probably should build hooks to catch certain bash commands, for example. That's very simple to do, but YOLO mode is the way to go for best results. Pi is self-aware. Here's how you actually use Pi. You describe what you need in plain language. This could be a scale, a tool, an extension, anything you want to build on top of the agent's harness, and then Pi just builds it. Pi is designed to improve itself. It can read its own code, edit its own extensions, refine its own skills. There's no sandbox separating the agents from its own internals. It is fully self-aware. It knows what it is, how it works, and what it can modify. This is only possible because there are no restrictions, no hidden layers, no lockdown internals. The agents has full access to itself. The agents is both the builder and the thing being built. Open Claw, you know what Open Claw is? A clawed bot, Multibot. One of the most popular and fastest-growing open-source agent products from the start of this year. Open Claw is built on Pi. Pi is the underlying agent architecture in Open Claw. So, Pi is an agent, but it also gives you the architecture to build agents on top of. You don't need to reinvent the wheel. You don't need to build the agentic loop from scratch every single time you want to build an agent. Pi provides you the bare-bones agentic architecture that you can use to build all sorts of agents specialized to any task. This is what people are doing. People are building agentic AI products on top of Pi. That's the level of generality we're talking about. It's the perfect tool for building custom agents efficiently. Other coding agents hide their mechanics, Pi exposes them. You constantly work with context management, tool design, prompt engineering at the raw level. Pi and abstract these away. And one of the strengths of Pi is that it'll enhance and build your understanding. If you want to learn about AI agents and really build your understanding in the field, using Pi and building on top of Pi will help you do that. As you build with Pi, you will accumulate knowledge and your understanding of AI agents will start to compound. Also, very important, you're not locked in to any one provider. Pi supports 15 plus providers and hundreds of models. And, of course, you can add support to custom providers and models yourself. So, you're not locked in to any one provider. If a better model drops tomorrow, you can just swap it in. If a better approach to stool design emerges, you implement it right away. You can stay ahead and keep your edge regardless of which model or harness leads the market in two years. AI agents will be the defining tools of the next decade. And this is an edge worth investing in. Claude code, cursor, open claw, they're all mainstream now. And they're all good tools. They They work really well. But, a tool on everyone's machine stops being an edge. So, if you care about getting the maximal edge in the space, to me at least, Pi is the obvious choice.
