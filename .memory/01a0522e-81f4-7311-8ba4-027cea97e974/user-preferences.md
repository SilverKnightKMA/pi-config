---
id: user-preferences
title: User communication and working-style preferences
summary: Language (Vietnamese), exhaustive-testing demand, preference for guided interactive instructions over forced interactive runs, scope of features to keep covered, model-audit verdict, stop-bug interaction patterns, and priority-on-user-action directive.
updated: 2026-08-30 15:36
---

- **Language**: user writes in Vietnamese; assistant's outputs to the user are typically Vietnamese. Internal tool output and code stay in English. Quoted user phrases are kept verbatim in Vietnamese.
- **Testing scope**: when the user asks for testing, they want EXHAUSTIVE coverage of every tool/skill/extension, including error paths and all modes. Surface-level or "shallow" testing has been explicitly criticized (verbatim: "test hơi sơ sài" — "the testing is a bit shallow").
- **Interactive handling**: for tools/features that require user interaction (e.g. `/snip`, `/md-log <​filepath>`, `/om`, live quiz/ask_user_question), do NOT force the user through them; instead emit step-by-step INSTRUCTIONS for the user to run themselves. After the audit, the user actually pressed the user-side commands (`/om:status` at 14:14, `/md-log test-log-2.md` at 14:16) and confirmed they work.
- **Priority directive (NEW 14:00)**: items needing USER action should be done FIRST; guided-choice spawn (item E) and user-side commands (item F) deferred, guided-choice spawn deferred to LAST since it blocks the turn until the user answers via `ask_user_question`.
- **Quoted directive** (verbatim, 10:25): "Ok, nhưng test hết toàn bộ các trường hợp có thể xảy ra của tool/skill/extension nhé, mình thấy test hơi sơ sài, những cái gì cần interactive thì response hướng dẫn cho mình nhé."
- **Cleanliness**: agent-history cleanup is the user's call; the assistant leaves spawned test subagents in place by default and only suggests archiving when relevant.
- **Paseo daemon**: the assistant must never restart the Paseo daemon (kills the running session agent); the user has reinforced this rule three times.
- **Test artifacts**: the user is comfortable with small PNG viz files left in `viz/` as evidence; ad-hoc `/tmp` test directories are cleaned at session end.
- **Visual design**: the user prefers the main visual path to be Obsidian via `md-log` with `![[viz-...png]]` embeds (the Paseo chat timeline cannot render custom images — `pi.sendMessage` is text-only).
- **Diagnostic discipline**: when proposing a fix the user pushes back on stop-gap solutions that risk data loss ("inter-agent communication is important and must not fail due to an unstable channel"); wants events surveyed before committing to one; wants the upstream original studied for reuse before porting.
- **Scope auditing**: the user asks whether a port imported UX expectations from the wrong source repo ("làm nhầm scope") — i.e. wants the assistant to verify that ported extensions stay within the scope of their upstream counterparts and not silently inherit behavior from a different upstream project.
- **Final verification protocol (13:25)**: user asked in Vietnamese ("tất cả PoCs OK chưa, dùng hết tools/skills/extensions có lỗi không, có vấn đề gì không, có thiếu/duplicate gì không, test case có cover đủ không") — wants an honest pass/fail report covering all four axes, with explicit "no open items" or a list of gaps, never a soft glossing-over.
- **Approve-all work directive (13:12)**: "Ok làm hết đi" — user greenlights completing every outstanding cleanup item without further confirmation gates; expects assistant to enumerate open items, address them, and report.
- **Key/provider trust**: user expects the assistant to re-verify earlier "missing key" / "quota exhausted" claims when the user knows a prior agent already filled those keys (13:18: "mình thấy có agent cũ đã điền rồi"). Don't repeat stale negatives.
- **Provenance skepticism (13:18)**: user pushed back on the assistant's claim that `visual-tools` / `web-fetch` / `md-log` had "no dev copies" by asking "chúng không xuất hiện từ hư không" ("they didn't appear from thin air") — wants concrete upstream-repo attribution, not a shrug.
- **Model evaluation (13:47)**: user asked for an audit of past runs of `deepseek-v4-flash`, `MiniMax-M3`, `glm-5.2`, `glm-5.3`, `glm-5.3-flash` on Paseo for call errors, thinking chains, and problem handling — with special suspicion of `MiniMax-M3` stability (since multiple scout-role agents today used it: `3ef0e69e`, `151c7540`, `a20980ef`, `345f36ad`, `5cd46180`). Delegated to background worker `3108961a-a28d-4edf-97e7-35ebc690613c` named `model-audit` running the `analyze-sessions` skill. Audit report received 14:00 (151 session files, agent `3108961a`).
- **Model audit verdict (14:00)**: `glm-5.3` ranked BEST (437 calls, excellent thinking, diagnosed root causes, 8 API errors mostly intentional STOP/PoC); `glm-5.3-flash` good but wanders when ambiguous (4 errors, recovers cleanly, cheap, HAS vision); `MiniMax-M3` stable API (0 API errors, 0 aborts) but a FORMAT BUG leaking raw `'thinking'` into the text channel ~80% of calls causing Paseo double-parse garbage (the "not stable" symptom the user saw; matches stray response fragments like text block `151c7540` ending with `'contents.'`); `deepseek-v4-flash` reliable budget worker (4+5 errors, self-recovers, good for cheap observer/probe roles); `glm-5.2` not run today (no fresh evidence). Recommendation: switch scout role model from `MiniMax-M3` to `glm-5.3-flash` (clean format + has vision, same `[V]` tag, cheaper). One-line change in `scout.md` awaiting user approval. Note: NOT a vision issue (both have `[V]`); it's the think-leak format defect.
- **Stop-abort interpretation (13:43)**: user reported the `System Error 'This operation was aborted (stopReason=error, model=cli-openai/zaicp/glm-5.3)'` from a paused sleep; initially suspected it was caused by the OM consolidator. Assistant clarified it was the user's STOP button (same signature as the earlier interrupt PoC against the `sleeper` worker) — user accepted the explanation. The 21:18 stop was case-A (silent long tool execution, `sleep 50`) — incorrect because the agent was only in the sleep, causing a System Error abort and lost turn progress.
- **Stop-bug interaction pattern (14:10)**: user asks "Tại sao còn pause?" when the spinner keeps spinning/timer keeps running after a complete answer. Assistant's golden rule for the stop bug: when answer looks complete and spinner still spins, press STOP immediately; nothing is lost (the turn already finished) and it frees stuck messages; sending a new message also resets the stuck spinner. User stopped the agent twice earlier (around 13:44 and 13:57/14:00) thinking it was stuck; both times the agent was actually still in long-tool execution or had completed and was just waiting for a dropped finish notification.
- **Repeated-stop clarification (14:10)**: user clarified the pause happened right after a complete answer (turn ended cleanly with `stopReason 'stop'`, no extension handler hung, no `extension_error`), not during any tool. Their time was 9:09 PM GMT+7 on 2026-08-30. Session log evidence: two `'Request aborted'` entries (13:57:14 and 14:00:41, 0 tokens) were the user's stops; a normal tool error -32602 for `list_models` at 14:03:33; no `extension_error` lines anywhere in the 888-line session file.
- **Search-engine trust (NEW 14:14)**: after the OM fix, user asked if the memory index appearing in a nested dir is a bug or intended. Assistant clarified it's the intended per-session layout (2 levels deep from project root) and `find` confirmed no nested `.memory` remained.
